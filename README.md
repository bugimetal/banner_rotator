# Banner rotator

## Installation
    $ pip install PIL
    $ python manage.py syncdb
    $ psql rotator -U postgres < rotator.sql
    $ python manage.py runserver

## Admin credentials
Login: admin
Password: test